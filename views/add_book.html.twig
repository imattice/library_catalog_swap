<html>
    <head>
        <title>Home</title>
    </head>
    <body>
        <h1>Add Book</h1>
        <a href="/">Home</a>
        <form action="/add_book" method="post">

            <label for="author_first">Author First Name:</label>
            <input type="text" name="author_first" id="author_first">

            <label for="author_last">Author Last Name:</label>
            <input type="text" name="author_last" id="author_last"><br>

            <label for="title">Title:</label>
            <input type="text" name="title" id="title">

            <button type="submit">Submit</button>
        </form>

        {%  if books is not empty %}
            <ul>
                {% for book in books %}
                    <li><a href="/books/{{ book.getId }}/edit">{{ book.getTitle }}</a> - {{ book.getAuthorLast }}</li>
                {% endfor %}
            </ul>
        {% else %}
            <p>There are no books yet!</p>
        {% endif %}

        <form action="/delete_books" method="post">
            <button type="submit">Delete All Books</button>
        </form>
    </body>
</html>
